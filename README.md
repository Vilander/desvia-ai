Project scaffold for Desvia AI mobile app.

Structure created under `src/` with components, screens, navigation, theme and assets.

Run with Expo or React Native CLI. Install dependencies:

```bash
npm install
npm install @react-navigation/native @react-navigation/stack react-native-safe-area-context react-native-svg
```
